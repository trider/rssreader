ionic run android --device;adb logcat | grep -i "console"


   lintOptions {
          checkReleaseBuilds false
          // Or, if you prefer, you can continue to check for errors in release builds,
          // but continue the build even when errors are found:
          abortOnError false
      }


ionic build android --release
/Users/jonnygold/tnmClient/platforms/android/build/outputs/apk

jarsigner -verbose -sigalg SHA1withRSA -digestalg SHA1 -keystore pcs-release-key.keystore traknow-release-unsigned.apk pcs

password pcsrcs

zipalign -v 4 traknow-release-unsigned.apk traknow.apk





